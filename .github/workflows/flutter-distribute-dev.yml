# Flutter Dev Distribution
# Triggers on push to dev branch
# Builds APK and distributes to Firebase App Distribution

name: Flutter Distribute Dev

on:
  push:
    branches:
      - dev
    paths:
      - 'frontend_mobile/**'
      - '.github/workflows/flutter-distribute-dev.yml'

jobs:
  distribute:
    name: Build and Distribute Dev
    runs-on: ubuntu-latest
    environment: dev
    defaults:
      run:
        working-directory: ./frontend_mobile

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build Release APK
        run: flutter build apk --flavor dev -t lib/main_dev.dart --release

      - name: Upload APK to Firebase App Distribution
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID_ANDROID_DEV }}
          serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEV }}
          groups: internal-testers
          file: frontend_mobile/build/app/outputs/flutter-apk/app-dev-release.apk
          releaseNotes: |
            Dev build from commit ${{ github.sha }}
            Branch: ${{ github.ref_name }}

      - name: Upload Crashlytics mapping file
        if: success()
        run: |
          # Install Firebase CLI
          npm install -g firebase-tools

          # Upload mapping file for Crashlytics symbol resolution
          MAPPING_FILE="build/app/outputs/mapping/devRelease/mapping.txt"
          if [ -f "$MAPPING_FILE" ]; then
            firebase crashlytics:mappingfile:upload \
              --app=${{ secrets.FIREBASE_APP_ID_ANDROID_DEV }} \
              --token="${{ secrets.FIREBASE_TOKEN_DEV }}" \
              "$MAPPING_FILE"
          else
            echo "No mapping file found at $MAPPING_FILE (expected for debug builds)"
          fi

      - name: Distribution Summary
        run: |
          echo "## Distribution Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment:** Dev" >> $GITHUB_STEP_SUMMARY
          echo "- **Flavor:** dev" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Tester Group:** internal-testers" >> $GITHUB_STEP_SUMMARY
